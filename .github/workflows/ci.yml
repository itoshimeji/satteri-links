name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [22, 24]
    steps:
      - uses: actions/checkout@v6
      - uses: voidzero-dev/setup-vp@v1.17.0
        with:
          node-version: ${{ matrix.node-version }}
          cache: true
      - run: vp check
      - run: vp test
      - run: vp pack
        working-directory: packages/link-preview
      - run: vp pack
        working-directory: packages/link-card
      - run: vp pack
        working-directory: packages/link-mention
